# DEPRECATED — this directory has been removed.
# Use the flat structure instead:
#   infrastructure/   (VPC, security groups, MSK)
#   gateway/          (Konnect control plane + ECS + NLB)

terraform {
  required_version = ">= 1.0"
}

